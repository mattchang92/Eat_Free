$('.expired-token-warning').css('top', ($(window).height() * 0.4) + 'px');
$('.expired-token-warning').css('left', ($(window).width() * 0.5) - (0.5 * $('.expired-token-warning').width()) + 'px');
$('.expired-token-warning').show();
setTimeout(function(){
  $('.expired-token-warning').fadeOut('slow');
},10000)
